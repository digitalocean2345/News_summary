runtime: python311

# Use uvicorn workers for FastAPI compatibility
entrypoint: gunicorn -k uvicorn.workers.UvicornWorker -b :$PORT simple_health:app

env_variables:
  ENVIRONMENT: production

automatic_scaling:
  min_instances: 1
  max_instances: 5 